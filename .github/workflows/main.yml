on:
  push:
    branches:
      master

name: 'Trigger: Push action'

jobs: 
  stylecheck:
    name: stylecheck
    runs-on: ubuntu-latest
    steps:
    - name: Build image
      uses: btford/write-good@master
    - name: Run stylecheker
      run: write-good *.md
