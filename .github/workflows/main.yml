on:
  push:
    branches:
      master

name: 'Trigger: Push action'

jobs: 
  stylecheck:
    name: stylecheck
    runs-on: ubuntu-latest
    steps:
    - run: pwd
    - run: ls -la
    - run: npx write-good index.md
